modelCheck('pines-independentmodel.txt')
modelData('pines-independentdata.txt')
modelCompile(1)
modelInits('pines-independentinits.txt')

modelUpdate(10)
samplesSet('theta')
samplesSet('X')
modelUpdate(100)
samplesStats('*')

